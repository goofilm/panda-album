- Android 应用 ID、命名空间、版本信息均通过 `flutter.*` 变量从 Flutter 侧注入，避免在 Gradle 中硬编码
- 权限声明按 Android 版本分层注释（Android 13+ / Android 14 / Android 12及以下），体现渐进式 API 兼容策略
- 构建产物统一输出到项目根目录的 `build/` 文件夹下，通过 `rootProject.layout.buildDirectory` 集中管理