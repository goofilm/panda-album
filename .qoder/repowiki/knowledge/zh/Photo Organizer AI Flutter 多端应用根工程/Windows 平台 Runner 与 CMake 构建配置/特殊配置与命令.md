使用 CMake 构建，需先通过 `flutter build windows` 生成 `flutter/` 下的 ephemeral 文件和 `build/windows` 产物；CMake 配置默认支持 Debug、Profile、Release 三种构建类型，Profile 模式复用 Release 链接器标志；VS 集成时 `CMAKE_VS_INCLUDE_INSTALL_TO_DEFAULT_BUILD=1` 使得默认构建即完成 install 步骤。